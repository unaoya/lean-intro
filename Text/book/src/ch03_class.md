# Ch3 + と 0 はどこから来るか — class と instance

<!-- 下書き（配置設計版）: 節立てと内容の配置メモのみ。本文未執筆 -->

- 前章からの問い: `a + b` の `+` はどこから来たか
- 到達点: インスタンス解決が「見える」。`(2 : Real)` がエラーになる理由が言える
- 新しい Lean 機能: class・instance・インスタンス解決（双子章・前編）
- コード: 新ファイルなし（C02 を素材に教える章）

## 3.1 根幹の 2 行

- `axiom Real.instLOF` ＋ `instance : LinearOrderedField Real := Real.instLOF`——「Real 上の順序体構造一式」という 1 個のレコードを公理で名指しし、正準登録する

## 3.2 インスタンス解決の機構

- `a + b` の型検査が `HAdd.hAdd → instLOF` を辿る過程を `#check` と set_option で観察
- 暗黙引数の穴埋めと同じ機構（Ch6 の apply=単一化への伏線）

## 3.3 エラーを読む: `#check (2 : Real)`

- failed to synthesize `OfNat Real 2`——機構がエラーとして見える実演。**Ch8 で回収する伏線**
- 0 と 1 だけインスタンスがある理由（Σ の基底・半分の構成——需要駆動）

## 3.4 双子章・前編: class の設計基準

- 「その型に対して値が正準に 1 つなら class」。リトマス試験表の class 側を埋める（structure 側は Ch5 で）
- コラム: ダイヤモンド事件（NatCast の二重インスタンス）

## 演習

- 公理設計の論点の追体験: 「0 を ∃ で置いたら何が壊れるか」を議論（コードは Ch11 以降の choose 学習後に再訪）

## 引き

- 「リーマン和には Σ が要る。有限和とは何か？」
