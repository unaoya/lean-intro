# Ch2 数学的構造と class の仕組み

<!-- 下書き（配置設計版）: 節立てと内容の配置メモ。machinery 章（2026-06-15 スワップで Ch3 から入替） -->

- 前章からの問い: 実数とは何か（→ 実数は「構造」として与えられる。まず構造とは何か・Lean でどう書くかを掴む）
- 到達点: 「構造＝データ・class＝自動で見つかる構造」の見方を得る。一般構造で 1 回証明して複数インスタンスに適用する経験
- 新しい Lean 機能: structure キーワード・署名の読み方・暗黙引数（読む）・class（最小: 自動解決される構造）
- コード: C02_Structures.lean（structure_as_data・general_proof・And は structure）

> 執筆メモ（ユーザー・relocate 元 ch02）:
> - 集合とその上の群構造という定義をする。より正確には集合とその上の演算と結合律・単位律・逆元。
> - これらを性質として述べるか、構造とするか。普段はあまり区別しないが、構造とした方がいい？（選択公理との関係？ → 深い理由は後章、ここは予告）
> - Add や Neg・AddCommGroup・Zero などは全て型である。集合っぽくいうと、X に対して X 上の適切な演算の集合を対応させる写像。同じ集合でも群構造の定めかたはたくさんある。
> - Nat は既存のものがある。Nat にモノイド構造を載せてみよう。素朴には加法モノイドと乗法モノイドが定義できる（→ §2.3 の general_proof）。

> 📝 レビュー指摘（2026-06-15・要対応）:
> - **構成**: 現 §2.1 末尾の「この章で説明したいこと」（Nat の +/× が交換則を満たす→直接証明も可だが可換モノイドの一般性質から導ける→構造を定義して導く）は**章の thesis**。メタ情報の直後（章頭）へ移すと読者が最初に地図を持てる。
> - **精度①**: 「`Add α` ＝ `Map(α×α, α)`」は厳密には誤り。`Add α` は二項演算を 1 つ持つ**1 フィールドのレコード**で、関数型 `α→α→α` と*同型*。「と思える／本質的に同じ」と書けば正確。
> - **精度②**: `#check Add` の出力は `Add.{u} (α : Type u) : Type u`。`.{u}`（universe）は Ch3 の話題なので、散文では「`Type → Type`」と読み下し universe には触れない。
> - **接続**: L39「op に notation つけられる？ class じゃないとダメ？」は**そのまま §2.5→§2.6（class）の動機**。固定構造には記法を当てられる（`local_notation` デモ追加済）が、多相な `+` は class が要る、と回収する。
> - **格下げ**: bundled 版（台集合もフィールド・L41–42）は本線でなく 🪟「理論の窓」へ。本書が使うのは **unbundled**（台 α はパラメータ・構造は α の上のデータ）。
> - **解決済（calc）**: L44–45 の「calc／term vs tactic」懸念は、**calc を Ch1 に移して** `nat_interchange`（加法版）を直接証明したことで解消。Ch2 の `interchange` は calc 既知で読める（証明は term mode・`by` は Ch6）。乗法版の直接証明は **Ch1 の演習**。

## 2.1 型は集合、構造はデータ

- Ch1 では型は「命題」だった。ここでは型は「住人を持つ集合（carrier）」の顔
- 構造＝データ: 演算と「それが法則を満たす証明」を 1 つの項に束ねたもの——**Ch1 の `⟨h.2, h.1⟩` ペアの大きい版**。`structure` キーワードでこういうレコード型を作れる
- 署名の読み方（∀・→・暗黙引数 `{}`）は構造のフィールドを読むのに要る分だけここで

Addの定義は何か？
Add aはa上の二項演算全体の集合と思える。
要するにMap(a \times a, a)のこと。

Mulもある。
これも集合としては同じ。
なぜ二つあるか？

notationを区別する。
環などの二つの演算がある場合。
（乗法とか加法とかいうのは気持ちの問題）

構造の上に構造を重ねることができる。
性質も構造と思う。
二項演算付き集合、結合的二項演算付き集合、etc

notationの説明。
opにnotationつけれる？それともstructureじゃなくてclassじゃないとだめ？

代数構造のbundle版について。
台集合もメンバーとする定義。

interchangeの証明でcalc記法を使っている。
これの説明は？term modeとtactic modeの説明との関係性。

このchで説明したいこと。
自然数全体の集合はNatという型に対応する。
自然数上の二項演算はたくさんある（それ全体の集合はMap(N \times N, N)である。）が、その中で馴染みのあるものに、通常の加法と乗法がある。
(a+b)+(c+d)=(a+c)+(b+d)を満たすし、(a.b).(c.d)=(a.c).(b.d)を満たす。
これらを直接証明することも可能（練習問題）だが、
一方でこれらはいずれも可換モノイドの一般的性質から導くことができる。
そこで、可換モノイドという代数構造を定義しその基本性質を証明した上で、
自然数の加法と自然数の乗法がいずれも可換モノイドであることを示し、
そこから導く、という方法を紹介する。
そのために必要なleanの機能も紹介する。

## 2.2 「α 上の構造」は型・住人が構造ひとつ（ANCHOR `structure_as_data`）

- `#check (Add Nat)` → `Add Nat : Type`。`Add α` の住人は「α 上の二項演算ひとつ」
- `example : Add Nat := ⟨Nat.add⟩` と `⟨fun a b => a*b⟩`——乗法すら住人になれる。**Add は法則を持たない**から
- 同じ集合 Nat に構造は複数載る。法則は構造の側（次節）が束ねる

## 2.3 一般構造で 1 回証明する（ANCHOR `general_proof`）

- `structure CommMonoidStr (α) where op; op_assoc; op_comm`（演算＋法則の証明を束ねる）
- 交換則 `(a⋆b)⋆(c⋆d) = (a⋆c)⋆(b⋆d)` を結合律＋可換律**だけ**から証明（公理ではない・**Ch1 の `nat_interchange` の抽象版**）。calc は Ch1 既出なので証明がそのまま読める
- **具体→抽象の回収**: Ch1 で `Nat.add` について直接証明した式を、ここでは構造の言葉で 1 回証明
- `natAdd`・`natMul`（Nat の 2 つの可換モノイド・法則は core の定理を渡すだけ）→ 1 回の証明が `+` と `*` の両方に効く（`interchange natAdd …` が具体形に defeq 一致）。**乗法版を直接書く労力（Ch1 演習）が、ここで無料になる**
- **抽象化の威力**: structure を引数に渡すだけ——instance も class もまだ要らない

## 2.5 記法は付けられるか？（ANCHOR `local_notation`・L39 の回収）

- 固定した構造 `natAdd` には局所記法 `⋆` を当てられる（structure のままで可能・`interchange natAdd` がそのまま使える）
- だが `⋆` は「natAdd という特定の構造」に固定。型ごとに構造を自動で選ぶ多相な `+`（ℝ でも Nat でも `a+b`）は作れない——構造を探す仕組みが要る → 次節 class

## 2.6 class＝自動で見つかる構造（最小導入）

- `M.op a b`／固定記法は煩雑。ℝ では `a + b` と書きたい→それを叶えるのが `class`／`instance`＝「構造を Lean が自動で探して渡す」仕組み。だから次章 ℝ の公理は `class` で書く
- ⚠ ここは**最小限**: 「class = 自動で見つかる structure」だけ。解決の深い機構・`(2:Real)` エラー・ダイヤモンド事件は後章（Ch3 で軽く観察、深い話と diamond は Ch8）

## 2.7 種明かし: `⟨⟩` も structure だった（ANCHOR `and_is_structure`）

- `#print And` → And は structure。Ch1 で使った `⟨h.2, h.1⟩` の `⟨⟩` の正体（Ch4 帰納型・Ch5 Partition へ繋ぐ）

## 演習

- `Add Nat` の住人をもう 1 つ作る・`CommMonoidStr` の別インスタンス（Bool の ∧/∨ 等）・交換則を別の構造に適用
- structure 設計: `n` を implicit にできるか（Ch4 Summation への布石）
- 🪟 窓候補: bundled 版（台集合もフィールドにする構造）と unbundled（本書の流儀）の対比

## 引き

- 「道具は揃った。実数の公理を読もう——それは『構造』として書かれている」
